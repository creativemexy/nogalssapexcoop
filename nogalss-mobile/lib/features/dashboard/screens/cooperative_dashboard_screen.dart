import 'package:flutter/material.dart';

class CooperativeDashboardScreen extends StatelessWidget {
  const CooperativeDashboardScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Cooperative Dashboard'),
      ),
      body: const Center(
        child: Text('Cooperative Dashboard - Coming Soon'),
      ),
    );
  }
}
