import 'package:flutter/material.dart';

class ApexDashboardScreen extends StatelessWidget {
  const ApexDashboardScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Apex Dashboard'),
      ),
      body: const Center(
        child: Text('Apex Dashboard - Coming Soon'),
      ),
    );
  }
}
