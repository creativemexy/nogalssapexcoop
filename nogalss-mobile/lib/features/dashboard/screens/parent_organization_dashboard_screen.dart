import 'package:flutter/material.dart';

class ParentOrganizationDashboardScreen extends StatelessWidget {
  const ParentOrganizationDashboardScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Parent Organization Dashboard'),
      ),
      body: const Center(
        child: Text('Parent Organization Dashboard - Coming Soon'),
      ),
    );
  }
}
